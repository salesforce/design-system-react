$('#picklist').facades_picklist({
    collection: [
        { text: 'one', value: 1 },
        { text: 'two', value: 2 }
    ],
    selection: { value: 1 }
});

$('#picklist').on('changed', function(evt, item) {
    console.log(item);
});