$('#pills').facades_pillbox({
    selection: [
        { text: 'one', value: 1 },
        { text: 'two', value: 2 },
        { text: 'three', value: 3 }
    ]
});

$('#pills').on('changed', function(event, item) {
    console.log(item);
});