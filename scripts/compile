rm -rf dist/
mkdir -p dist/
rm -rf lib/
mkdir -p lib/

BABEL="babel -d lib/ components/"
BUILD="webpack --devtool source-map --config webpack.build.config.js"

eval $BABEL
#eval $BUILD
#eval "MINIFY=1 $BUILD"