/**
 * This is an empty file to run eslint on in order
 * to validate the config, since prettier-eslint
 * will ignore invalid eslint configs and go ahead
 * with prettier settings by itself which will change
 * hundreds of files in the case of `npm run lint:fix`.
 * Yeah, not fun...
 */
